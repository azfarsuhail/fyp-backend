# --- Core API & Server ---
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic[email]>=2.6.0
python-multipart>=0.0.9
python-dotenv>=1.0.0

# --- Database & ORM (Neon DB) ---
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9
alembic>=1.13.0

# --- Security & Authentication ---
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1  # Pinned: newer bcrypt versions break passlib
python-jose[cryptography]>=3.3.0

# --- AWS Cloud Integration ---
# Required for connecting to S3 for image uploads and the video library
boto3>=1.34.0

# --- Machine Learning & Image Processing (Diagnostic Agent) ---
# Note: Using tensorflow-cpu keeps the Docker image much smaller if you aren't doing GPU inference inside the container. 
# If you deploy to an AWS G4 instance and want GPU acceleration, change this to `tensorflow`.
tensorflow>=2.15.0
Pillow>=10.2.0
numpy>=1.26.0

# --- RAG & NLP (Recommendation Agent) ---
# Required to process the sentence-transformer model for the vector database
sentence-transformers>=2.3.0

# --- HTTP Client ---
# Used to download images from S3 URLs for processing
requests>=2.31.0

# --- Testing ---
pytest>=8.0.0
httpx>=0.27.0